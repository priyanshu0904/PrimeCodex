# Introduction
## About Java
- One of the popular language runs across 60,00,00,00,000 devices.
- Wide usage - Web apps, Backend, Mobile apps, Enterprise software.
- Object Oriented
- Rich API and Community Support.

---
## Programming Language
- Computers only understand 0/1 or ON/OFF or machine code.
- Computer is a dumb machine.
- Giving instructions to a computer. Instruction tells the computer what to do. Instructions are also called **Code**. High level languages.
- Uses compiler to convert the High level language (Programming languages like java python etc) to Low level language or machine  code.

> Step-by-step procedure or instructions to solve a problem or performing a  specific task is called **Algorithms**.

> Rules of the language or writing anything in proper structure is **Syntax**. For programming exact syntax must be followed otherwise it will through an error.


---
## History of Java
- Developed by **James Gosling** at Sun Microsystems in early 1990s.
- Firstly named as 'Oak', later named Java in 1995 (First Release). Oak is a tree. Java is island where the coffee beans is famous.
- **"Write Once, Run Anywhere"** - basic principle of Java with cross-platform compatibility.
- Developed with vision of **Backward Compatibility**. Should not break with new version release. This is the property of coffee that's why the logo of Java is Coffee cup.
- From 1990 - 2010, rapid growth and diversification. Standardized in different editions for various computing platforms. From web applets (UI) to server side applications.
- In 2008-09, occupied by **Oracle**.
- After 2018-19, many Java versions had come.
- It also helps to change the Internet because both are rising simultaneously. It provides Portability with the **Write once run anywhere** and Security because of **Code running on Virtual machine**.

- C/C++ directly compiles to machine code and that can not be run on different machines.
- Java/Python uses bytecode concept. JVM converts machine specific machine code or LLL.

---
## ByteCode
- The main concept of Java which makes it unique.
- ByteCode is platform independent.
- Human can not understand or not human readable.
- Not the machine code or 0/1.
- The java compiler converts the source code into ByteCode. After that the **JVM** (Java Virtual Machine) of the particular machine converts to the machine instruction of that particular machine from that bytecode. This process is interpreter. 
- Bytecode must not be changed.
- It provides security because it runs on Virtual Machine.

---
## Buzzwords
- **Robust :** Java is robust due to its strong memory management, exception handling, and type-checking mechanisms, which help in preventing system crashes and ensuring reliable performance.

- **Multithreaded** : Multithreading in programming is the ability of a CPU to execute multiple threads concurrently, allowing for more efficient processing and task management.

- **Architecture Neutral** : Java is architecturally neutral because its compiled code (bytecode) can run on any device with a Java Virtual Machine (JVM), regardless of the underlying hardware architecture.

- **Interpreted and High Performance** : Java combines high performance with interpretability, as its bytecode is interpreted by the Java Virtual Machine (JVM), which employs Just-In-Time (JIT) compilation for efficient and fast execution.

- **Distributed** : Java is inherently distributed, designed to facilitate network-based application development and interaction, seamlessly integrating with Internet protocols and remote method invocation.




