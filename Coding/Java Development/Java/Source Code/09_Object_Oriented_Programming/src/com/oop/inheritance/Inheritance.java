package com.oop.inheritance;

public class Inheritance {
    //child inherits properties from parents(existing class), grandparents....and so on, child have its own properties also
    //trick: child(sub-class) is a parent(super-class) -> if this is true then perform inheritance
    //if child has a parent -> if this is true then child will make an object of parent, no inheritance
    //helps in code reusability, child can use members of parent class
    //extend keyword is used
    //single, multi-level, hierarchical inheritance
    //can't have more than one parent, means no multiple inheritance
}
